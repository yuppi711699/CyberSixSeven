/**
 * Placeholder for v0.1.
 *
 * This package is deliberately empty: v0.1 is a skeleton and `student-web`
 * fetches nothing. It exists now so the workspace wiring (raw-TypeScript
 * `exports` + `transpilePackages`) is proven before there is anything to break.
 *
 * From v0.2 this is where the typed fetcher and the TanStack Query
 * `queryOptions()` / `mutationOptions()` factories live, so `student-web` and
 * `admin-web` share one key and one fetcher.
 */
export const API_CLIENT_PLACEHOLDER = '@cybersixseven/api-client' as const;
