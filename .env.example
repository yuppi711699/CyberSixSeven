# Copy to .env and fill in. .env is gitignored.
# Docker Compose loads this file automatically for ${POSTGRES_PASSWORD}.
# Spring Boot does NOT — source it before starting platform-api:
#   set -a && source .env && set +a

POSTGRES_PASSWORD=change-me
DB_PASSWORD=change-me
DB_URL=jdbc:postgresql://localhost:5432/cybersixseven
DB_USERNAME=cybersixseven

# Frontend (student-web)
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080
