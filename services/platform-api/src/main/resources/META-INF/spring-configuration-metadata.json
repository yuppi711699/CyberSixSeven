{
  "properties": [
    {
      "name": "app.cors.allowed-origins",
      "type": "java.lang.String",
      "description": "Comma-separated exact CORS origins allowed with credentials. Never '*'. Required via CORS_ALLOWED_ORIGINS in the repo-root .env file."
    }
  ]
}
